{
  "name": "workflow-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Static contracts and documentation governance for the Apps Script workflow repository.",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "check:root": "node static_smoke_check.js",
    "check:standalone": "node scripts/check-standalone.mjs",
    "check:mappings": "node --test tests/mapping-contracts.test.mjs",
    "check:docs": "markdownlint-cli2 AGENTS.md README.md CHANGELOG.md && node scripts/check-docs.mjs",
    "check:diff": "node scripts/check-diff.mjs",
    "check:negative": "node --test tests/validator-negative.test.mjs",
    "check": "node static_smoke_check.js && node scripts/check-standalone.mjs && node --test tests/mapping-contracts.test.mjs && markdownlint-cli2 AGENTS.md README.md CHANGELOG.md && node scripts/check-docs.mjs && node --test tests/validator-negative.test.mjs && node scripts/check-diff.mjs"
  },
  "devDependencies": {
    "@mermaid-js/parser": "1.2.0",
    "markdownlint-cli2": "0.23.2"
  }
}
